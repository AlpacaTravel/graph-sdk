# Removes a collection location from a collection.

mutation deleteCollectionLocation($id: ID!) {
  # Use the deleteCollectionItem() mutation to remove a record
  deleteCollectionItem(
    # Supply the collection location id
    id: $id
  ) {
    # Reponse type
    __typename
  }
}
